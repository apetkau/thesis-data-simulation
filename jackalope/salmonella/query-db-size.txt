SELECT table_name AS `Table`,round(((data_length) / 1024 / 1024), 2) as `Data size (MB)`, round(((index_length) / 1024 / 1024), 2) as `Index size (MB)` FROM information_schema.TABLES WHERE table_schema = "thesis";
